# OS junk
.DS_Store
Thumbs.db

# Logs & env
*.log
.env
.env.*

# Editors
.idea/
.vscode/

# dev-loop git worktrees (created inside the repo by the orchestrator; never committed)
.claude/worktrees/

# Raw design uploads (keep the extracted design/waldo-design-system/ instead)
design/*.zip
