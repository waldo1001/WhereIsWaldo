distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
distributionUrl=https\://services.gradle.org/distributions/gradle-9.4.1-bin.zip
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
# gradlew / gradlew.bat / gradle-wrapper.jar are intentionally NOT committed here: this sandbox
# has no JDK/Gradle toolchain to produce a verified wrapper jar, and a hand-fabricated binary
# would be worse than none. Whoever first runs a real toolchain against this project should run
# `gradle wrapper --gradle-version 9.4.1 --distribution-type bin` to generate them (specs/003
# §13.4; mobile/android/README.md).
